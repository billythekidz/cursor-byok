interface:
  display_name: "Local Mode Implementation Guidance"
  short_description: "Use this skill when resolving local mode implementation or architecture tasks"
  default_prompt: "Use $coding-guidance to resolve local mode issues."
